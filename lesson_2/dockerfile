# Наследуемся от образа alpine
FROM alpine

# npm вместо node, чтобы установить нужные зависимости
RUN apk add npm && \
  npm i -g http-server

# Ограничение работы контейнера (сервера) в указанной папке
VOLUME /home/ul_node
# (точка монтирования)

# Указание рабочей папки контейнера (сервера)
WORKDIR /home/ul_node

# Копирование файлов из текущей папки в рабочую
COPY ./ /home/ul_node/

# Подсказывает, какой порт использовать
EXPOSE 8080

# Единственная функция, которая и запускает целевую задачу образа
CMD http-server
